'use client';

import { FlashcardsTopicSelect } from '@/widgets/trainersPage/ui/';

export default function LevelOnePage() {
  return (
    <div className={'mt-4 flex items-center justify-center'}>
      <FlashcardsTopicSelect />
    </div>
  );
}
