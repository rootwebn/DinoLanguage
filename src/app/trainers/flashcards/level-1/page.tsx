'use client';

import { TopicSelect } from '@/entities/trainerLevel/ui/';

export default function LevelOnePage() {
  return (
    <div className={'flex min-h-[80vh] items-center justify-center'}>
      <TopicSelect />
    </div>
  );
}
